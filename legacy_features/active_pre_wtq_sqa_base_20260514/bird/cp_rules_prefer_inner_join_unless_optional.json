{
  "canonical_id": "cp_rules_prefer_inner_join_unless_optional",
  "task": "bird",
  "target_module": "rules",
  "theme": "conservative-prior",
  "tier": "A",
  "provenance": "creative",
  "requires": [
    "_section_rules"
  ],
  "conflicts_with": [],
  "primitive_edits": [
    {
      "func_type": "insert_node",
      "params": {
        "node_type": "rule",
        "parent_id": "0408c65f2e17",
        "payload": {
          "content": "Default to INNER JOIN between tables linked by a foreign key. Do not switch to LEFT JOIN unless the question explicitly asks for entities that may have no match (e.g., 'including those with no orders')."
        }
      }
    }
  ],
  "rationale": "creative cp_: tightens join-type commitment, opposite-signed counterpart to a permissive 'allow LEFT JOIN' rule."
}