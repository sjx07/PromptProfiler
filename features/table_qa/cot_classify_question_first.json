{
  "feature_id": "cot_classify_question_first",
  "canonical_id": "cot_classify_question_first",
  "task": "table_qa",
  "requires": ["_section_reasoning"],
  "conflicts_with": [],
  "primitive_edits": [
    {
      "func_type": "insert_node",
      "params": {
        "node_type": "rule",
        "parent_id": "ddaf0b0424be",
        "payload": {"content": "Start by labeling the question type in one word (lookup / aggregation / superlative / count / filter / comparison). Then apply a strategy matched to that type."}
      }
    }
  ]
}
